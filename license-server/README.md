# License Server

Lightweight Node/Express license server using SQLite. Provides simple API to create keys, validate them, and report usage.

Install and run:

```bash
cd "d:\hell on top\license-server"
npm install
npm start
```

Open `http://localhost:3000/admin.html` to create keys.
